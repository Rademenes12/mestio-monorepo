import 'package:flutter/material.dart';

import 'store_screenshot_placeholder.dart';

class Screenshot5Screen extends StatelessWidget {
  const Screenshot5Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StoreScreenshotPlaceholder(screenNumber: 5);
  }
}
