import 'package:flutter/material.dart';

import 'store_screenshot_placeholder.dart';

class Screenshot4Screen extends StatelessWidget {
  const Screenshot4Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StoreScreenshotPlaceholder(screenNumber: 4);
  }
}
