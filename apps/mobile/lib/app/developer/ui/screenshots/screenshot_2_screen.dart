import 'package:flutter/material.dart';

import 'store_screenshot_placeholder.dart';

class Screenshot2Screen extends StatelessWidget {
  const Screenshot2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StoreScreenshotPlaceholder(screenNumber: 2);
  }
}
