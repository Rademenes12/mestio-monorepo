import 'package:flutter/material.dart';

import 'store_screenshot_placeholder.dart';

class Screenshot1Screen extends StatelessWidget {
  const Screenshot1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StoreScreenshotPlaceholder(screenNumber: 1);
  }
}
