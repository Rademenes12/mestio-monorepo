import 'package:flutter/material.dart';

import 'store_screenshot_placeholder.dart';

class Screenshot3Screen extends StatelessWidget {
  const Screenshot3Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StoreScreenshotPlaceholder(screenNumber: 3);
  }
}
