# Intro

Twoim zadaniem jako agenta AI jest zrealizowanie poniższych poleceń.

CEL: Utworzenie pliku `android/key.properties`.

# Task

1. Sprawdź czy istnieje plik `android/key.properties`.
2. Jeżeli go nie ma, utwórz go na podstawie `android/key.example.properties`
3. Jeżeli jest, upewnij się, że posiada wszelkie klucze z `android/key.example.properties`

## FINISH
Poinformuj mnie o rezultatach. Poproś mnie, abym wymyślił i wypełnił pola `storePassword`, `keyPassword` tym samym, nowym hasłem. Powiedz mi też, żebym zapisał je gdzieś poza tym plikiem `android/key.properties` w bezpiecznym miejscu. Zasugeruj mi napisanie `next`. Kolejny krok: `10_init-upload-keystore-create.md`.

Nie przechodź dalej dopóki nie napiszę `next`.
Gdy napiszę `next`, przejdź do `docs/commands/00_init/10_init-upload-keystore-create.md`.
