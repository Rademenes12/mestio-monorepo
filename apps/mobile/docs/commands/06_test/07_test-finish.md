# ZADANIE: 07_test_finish

## CEL
Podsumowanie fazy testowania.

## KROKI DO WYKONANIA:
1. Sprawdź czy nie ma niezacommitowanych zmian — jeśli są, uruchom `flutter analyze`, napraw ewentualne problemy i zacommituj.
2. Poinformuj mnie, że faza testowania jest zakończona.
3. Jeśli testy integracyjne były tworzone i uruchamiane, podsumuj ich wynik.
4. Jeśli testy integracyjne zostały pominięte, napisz jasno, że etap testów opierał się na manualnej weryfikacji.
5. Powiedz mi, że aplikacja jest gotowa do publikowania.
6. Zasugeruj mi napisanie `next`.

## FINISH

Zaktualizuj `STATE.md`: ustaw etap `test` jako `✅ done`, ustaw `Ostatni zakończony etap` na `test`, ustaw `Aktualny etap` na `publish` i zostaw status etapu `publish` jako `⬜ not-started`.

Poinformuj mnie, że etap `test` jest zakończony i przechodzimy do etapu `publish`.

Zasugeruj mi otworzenie nowej konwersacji / nowej sesji / nowego chata i wklejenie polecenia:
`Wykonaj: docs/commands/07_publish.md`

Jeśli chcę kontynuować w tej samej rozmowie, niech napiszę `next`.

Jeśli napiszę `next`, dopiero wtedy zapoznaj się z plikiem `docs/commands/07_publish.md` — nie wcześniej!
