# ZADANIE: 23_publish_finish

## CEL
Zamknąć etap `publish` bez zmieniania statusu aplikacji w `AGENTS.md` ani `CLAUDE.md`.

## KROKI DO WYKONANIA:
1. Podsumuj krótko, że etap publikacji jest zakończony od strony przygotowania sklepów, buildów i materiałów.
2. Przypomnij mi, że faktyczny status aplikacji zależy od paneli App Store Connect i Google Play Console.
3. Zacommituj wszelkie pozostawione same sobie pliki w git, aby było czysto.

## FINISH
Zaktualizuj `STATE.md`: ustaw etap `publish` jako `✅ done`, ustaw `Ostatni zakończony etap` na `publish`, ustaw `Aktualny etap` na `onboarding` i zostaw status etapu `onboarding` jako `⬜ not-started`.

Poinformuj mnie, że etap `publish` jest zakończony.

Jeśli chcę przejść do kolejnego etapu dostępnego w tym repo, zasugeruj mi otworzenie nowej konwersacji / nowej sesji / nowego chata i wklejenie polecenia:
`Wykonaj: docs/commands/08_onboarding.md`

Jeśli chcę kontynuować w tej samej rozmowie, niech napiszę `next`.

Jeśli napiszę `next`, dopiero wtedy zapoznaj się z plikiem `docs/commands/08_onboarding.md` — nie wcześniej!
